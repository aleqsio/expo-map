# screenmap in CI

This guide moved into the main README, so there is one copy to keep correct:
**[Review pull requests in CI](../README.md#review-pull-requests-in-ci)**.

It covers what the PR comment gives a reviewer, the three ideas the design rests
on, prerequisites, setup, what each run does step by step, every Action input,
the supported agent providers, running the same pipeline locally, the files the
Action reads and writes, and the decisions baked in.

Related: [scrmap-format.md](scrmap-format.md) for the map bundle, and
[diff-scrmap-format.md](diff-scrmap-format.md) for the changes bundle.
